{
  "name": "bamgate-worker",
  "private": true,
  "scripts": {
    "build": "npm run build:wasm && npm run build:assets && npm run build:worker",
    "build:wasm": "~/.local/tinygo/bin/tinygo build -o ./build/app.wasm -target wasm -no-debug .",
    "build:assets": "cp $(~/.local/tinygo/bin/tinygo env TINYGOROOT)/targets/wasm_exec.js ./build/wasm_exec.js",
    "build:worker": "cp ./src/worker.mjs ./build/worker.mjs",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
